spring:
  datasource:
    url: 'jdbc:mysql://mysql-container-new/cycleRest'
    username: root
    password: sandhya@123
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        globally_quoted_identifiers: true
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  private.key: classpath:app.key
  public.key: classpath:app.pub
